"use client"; import { useState } from 'react'; import { Sidebar } from './Sidebar'; import { Topbar } from './Topbar'; export function DashboardShell({shopSlug,title,children}:{shopSlug:string;title:string;children:React.ReactNode}){ const safeSlug=(shopSlug||'riri-collection'); const [open,setOpen]=useState(false); return <div className='flex min-h-screen bg-[#FAF8F5]'><Sidebar shopSlug={safeSlug} open={open} setOpen={setOpen}/><div className='flex flex-1 flex-col min-w-0'><Topbar title={title} shopSlug={safeSlug} onMenu={()=>setOpen(true)}/><main className='p-5 md:p-8 space-y-6'>{children}</main></div></div> }