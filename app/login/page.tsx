"use client"; import { useState, useEffect } from 'react'; import { useRouter } from 'next/navigation'; import { getUsers, setCurrentUser } from '@/lib/store'; import { Input, Label } from '@/components/ui/input'; import { Button } from '@/components/ui/button'; import Link from 'next/link'; export default function Login(){ const router=useRouter(); const [email,setEmail]=useState(''); const [mounted,setMounted]=useState(false); useEffect(()=>{ setMounted(true); },[]); const login=()=>{ const users=getUsers(); const u=users.find(x=>x.email.toLowerCase()===email.toLowerCase()); if(u){ setCurrentUser(u); router.push('/dashboard'); } else { alert('No account'); router.push('/signup'); } }; if(!mounted) return <div className='p-10 text-center'>Loading...</div>; return <div className='min-h-screen flex items-center justify-center p-4 bg-[#FAF8F5]'><div className='bg-white rounded-[32px] p-10 max-w-md w-full shadow-xl'><div className='flex items-center gap-3'><img src='/logo.png' className='h-10 w-10 rounded-xl object-contain border bg-white'/><span className='font-bold'>Riri SaaS</span></div><h1 className='mt-6 text-[22px] font-bold'>Login</h1><div className='mt-6'><Label>Email</Label><Input value={email} onChange={e=>setEmail(e.target.value)} placeholder='you@gmail.com'/></div><Button onClick={login} className='mt-6 w-full h-12 rounded-full bg-[#6B21A8]'>Login → Dashboard</Button><p className='mt-4 text-center text-[12px] text-gray-500'>No account? <Link href='/signup' className='font-bold text-[#6B21A8]'>Create shop free</Link></p></div></div> }