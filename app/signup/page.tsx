"use client"; import { useState, useEffect } from 'react'; import { useRouter } from 'next/navigation'; import { signupUser } from '@/lib/store'; import { Input, Label } from '@/components/ui/input'; import { Button } from '@/components/ui/button'; import Link from 'next/link'; export default function Signup(){ const router=useRouter(); const [form,setForm]=useState({name:'',email:'',phone:''}); const [err,setErr]=useState(''); const [mounted,setMounted]=useState(false); useEffect(()=>{ setMounted(true); },[]); const submit=()=>{ if(!form.name||!form.email){setErr('Name email required'); return;} try{ signupUser(form.name, form.email, form.phone); router.push('/onboarding'); }catch(e:any){ setErr(e.message); } }; if(!mounted) return <div className='p-10 text-center'>Loading...</div>; return <div className='min-h-screen flex items-center justify-center p-4 bg-[#FAF8F5]'><div className='bg-white rounded-[32px] p-10 max-w-md w-full shadow-xl'><div className='flex items-center gap-3'><img src='/logo.png' className='h-10 w-10 rounded-xl object-contain border bg-white'/><span className='font-bold'>Riri SaaS</span></div><h1 className='mt-6 text-[24px] font-bold'>Create store</h1><div className='mt-6 space-y-4'><div><Label>Name *</Label><Input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder='Tola'/></div><div><Label>Email *</Label><Input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder='tola@gmail.com'/></div><div><Label>WhatsApp</Label><Input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder='080...'/></div>{err&&<p className='text-[12px] text-red-600 bg-red-50 p-3 rounded-xl'>{err}</p>}<Button onClick={submit} className='w-full h-12 rounded-full bg-[#6B21A8]'>Create →</Button><p className='text-center text-[12px] text-gray-500'>Have account? <Link href='/login' className='font-bold text-[#6B21A8]'>Login</Link></p></div></div></div> }